spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shopping_cart
    username: postgres
    password: saran3112
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true